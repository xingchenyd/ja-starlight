/* eslint-disable @next/next/no-html-link-for-pages */
import {jobs} from "../../data";

export default async function JobDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const job=jobs.find(j=>j.id===id)||jobs[Number(id)-1];if(!job)return <main className="detail-page"><a href="/opportunities">← 返回机会列表</a><h1>职位不存在或已下线</h1></main>;return <main className="detail-page"><header><a className="brand" href="/"><span>JA</span><b>星光计划</b></a><a href="/opportunities">← 返回机会列表</a></header><section className="detail-head"><div className="company-logo" style={{background:job.color}}>{job.logo}</div><div><span className="status">{job.status}</span><h1>{job.title}</h1><p>{job.company} · {job.city} · {job.mode} · {job.duration}</p></div><a className="primary-btn" href={`/workspace?role=student`}>登录并申请</a></section><div className="detail-grid"><article><h2>你将参与什么</h2><p>{job.summary}</p><ol><li>在导师支持下参与真实业务项目，完成清晰的阶段任务；</li><li>与团队协作，定期同步进展并完成项目复盘；</li><li>将实践成果沉淀为作品或经企业确认的成长经历。</li></ol><h2>我们期待这样的你</h2><ul><li>对 {job.tags.join("、")} 领域有真实兴趣；</li><li>每周可稳定投入 3–4 天，并对结果负责；</li><li>具备清晰沟通、主动学习与团队协作意识。</li></ul><h2>JA 机会保障</h2><p>企业与职位信息已经平台审核；申请状态可追踪；涉及简历查看和下载的操作均会记录。</p></article><aside><b>职位信息</b><p><span>工作城市</span>{job.city}</p><p><span>工作方式</span>{job.mode}</p><p><span>实践周期</span>{job.duration}</p><p><span>已申请</span>{job.applications} 人</p><div className="chips">{job.tags.map(t=><span key={t}>{t}</span>)}</div></aside></div></main>}
