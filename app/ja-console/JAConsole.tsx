/* eslint-disable @next/next/no-img-element, jsx-a11y/media-has-caption */
"use client";
import {useEffect,useMemo,useState} from "react";
import Link from "next/link";
import type {RichBlock} from "../data";

type RecordItem={id:string;kind:string;payload:Record<string,unknown>;updatedAt:string};
type Log={id:number;actorId:string;action:string;targetType:string;targetId:string;createdAt:string};
type Registration={id:string;activityId:string;activityTitle:string;answers:Record<string,string>;status:string;createdAt:string;reviewNote?:string};
type Field={id:string;label:string;type:string;required:boolean};
const reviewable=["job","activity","content"];
const contentCategories=["技能成长","活动分享","企业曝光","职业探索","简历面试","公益实践"];
const jobCategories=["产品运营","技术研发","数据分析","品牌内容","智能制造","金融与商业","项目实践","公益实践"];
const kindName=(kind:string)=>kind==="job"?"实习/项目":kind==="activity"?"成长活动":kind==="content"?"成长内容":kind==="blacklist"?"诚信记录":"企业资料";
const stateName=(state:unknown)=>state==="approved"?"已通过":state==="rejected"?"已退回":"待审核";
const orderOf=(item:RecordItem)=>Number(item.payload.sortOrder||0)||0;
const fieldOptions:Field[]=[{id:"name",label:"姓名",type:"text",required:true},{id:"phone",label:"联系电话",type:"tel",required:true},{id:"email",label:"常用邮箱",type:"email",required:true},{id:"school",label:"学校、专业与年级",type:"text",required:true},{id:"studentId",label:"学号",type:"text",required:false},{id:"expectation",label:"参加活动的期待",type:"textarea",required:false}];
const abilityOptions=["表达沟通","问题解决","行业认知","项目管理","创新思维","团队协作","社会责任","数据分析"];
const adminStarterBlocks:Record<"activity"|"content",RichBlock[]>={
 activity:[{id:"a1",type:"heading",title:"活动背景"},{id:"a2",type:"text",text:"说明活动面向谁、解决什么成长问题、学生需要完成什么任务。"},{id:"a3",type:"agenda",title:"09:00-09:30 签到｜09:30-11:00 主题体验｜11:00-12:00 复盘输出"}],
 content:[{id:"c1",type:"heading",title:"学习目标"},{id:"c2",type:"text",text:"说明这篇内容/视频可以帮助学生解决什么问题。"},{id:"c3",type:"card",title:"关联活动或机会",text:"可写入关联活动、企业或实习项目，后续数据库阶段只保存对象 ID。"}]
};

export default function JAConsole({operator}:{operator:string}){
 const [tab,setTab]=useState("pulse"),[records,setRecords]=useState<RecordItem[]>([]),[logs,setLogs]=useState<Log[]>([]),[registrations,setRegistrations]=useState<Registration[]>([]),[notice,setNotice]=useState(""),[selectedId,setSelectedId]=useState(""),[selectedRegistration,setSelectedRegistration]=useState<Registration|null>(null),[reason,setReason]=useState(""),[loading,setLoading]=useState(true);
 const load=async()=>{setLoading(true);const response=await fetch("/api/admin");const data=await response.json();setRecords(data.records||[]);setLogs(data.logs||[]);setRegistrations(data.registrations||[]);setLoading(false)};
 useEffect(()=>{let active=true;fetch("/api/admin").then(response=>response.json()).then(data=>{if(active){setRecords(data.records||[]);setLogs(data.logs||[]);setRegistrations(data.registrations||[]);setLoading(false)}});return()=>{active=false}},[]);
 const pending=useMemo(()=>records.filter(r=>reviewable.includes(r.kind)&&(!r.payload.reviewStatus||r.payload.reviewStatus==="pending")).sort((a,b)=>orderOf(b)-orderOf(a)||String(b.updatedAt).localeCompare(String(a.updatedAt))),[records]);
 const publicRecords=useMemo(()=>records.filter(r=>reviewable.includes(r.kind)&&r.payload.reviewStatus==="approved").sort((a,b)=>orderOf(b)-orderOf(a)||String(b.updatedAt).localeCompare(String(a.updatedAt))),[records]);
 const blacklist=records.filter(r=>r.kind==="blacklist");
 const selected=records.find(r=>r.id===selectedId);
 const decide=async(decision:"approved"|"rejected",settings?:{sortOrder:number;category:string;featured:boolean})=>{if(!selected)return;if(decision==="rejected"&&!reason.trim()){setNotice("退回时请填写具体修改意见");return}const response=await fetch("/api/admin",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({id:selected.id,decision,reason:reason.trim(),...settings})});const data=await response.json();if(!response.ok){setNotice(data.error||"审核失败");return}setNotice(decision==="approved"?"审核通过：该内容已按排序规则进入公开目录":"已退回：企业端会显示修改意见");setSelectedId("");setReason("");await load()};
 const title=tab==="pulse"?"平台活力看板":tab==="review"?"企业发布审核":tab==="publish"?"JA 内容发布":tab==="registrations"?"活动报名数据":tab==="records"?"平台内容排序":tab==="integrity"?"诚信管理":"审计日志";
 const description=tab==="pulse"?"用真实发布、报名和审核数据说明 Star Plan 的平台活性。":tab==="review"?"审核企业提交的实习/项目、活动与成长内容，并设置排序、分类和推荐位。":tab==="publish"?"由 JA 直接发布活动、文章或视频，并配置学生报名信息。":tab==="registrations"?"查看全部活动报名及学生填写的信息。":tab==="records"?"集中查看已提交和已发布内容的状态、分类、推荐和排序。":tab==="integrity"?"记录虚假信息、失约或其他诚信风险，后续可接入账号限制。":"记录后台的重要操作，便于追踪审核与发布过程。";
 return <main className="ja-console platform v2 ja-platform"><aside className="side"><Link href="/" className="side-brand official-side-brand"><img src="/media/ja-china-logo.jpg" alt="JA China"/><b>Star Plan</b></Link><nav><button className={tab==="pulse"?"active":""} onClick={()=>setTab("pulse")}><span>◈</span>数据看板</button><button className={tab==="review"?"active":""} onClick={()=>setTab("review")}><span>✓</span>统一审核 <i>{pending.length}</i></button><button className={tab==="publish"?"active":""} onClick={()=>setTab("publish")}><span>＋</span>内容发布</button><button className={tab==="registrations"?"active":""} onClick={()=>setTab("registrations")}><span>☷</span>报名数据 <i>{registrations.length}</i></button><button className={tab==="records"?"active":""} onClick={()=>setTab("records")}><span>▱</span>平台内容</button><button className={tab==="integrity"?"active":""} onClick={()=>setTab("integrity")}><span>!</span>诚信管理</button><button className={tab==="audit"?"active":""} onClick={()=>setTab("audit")}><span>◷</span>审计日志</button></nav><div className="side-help"><b>测试阶段</b><p>当前 JA 后台临时免管理员验证，便于企业内测；正式上线前恢复白名单。</p></div><a className="back" href="/signout-with-chatgpt?return_to=/">退出后台</a></aside><div className="work"><header className="topbar"><div className="workspace-context"><small>JA STAR PLAN OPERATIONS</small><b>湖南运营后台</b><span>内容审核、排序与成长数据</span></div><div className="top-actions"><span className="operator-name">{operator}</span><div className="avatar">JA</div></div></header><section className="workspace ja-workspace"><div className="page-title"><div><small>JA HUNAN OPERATIONS</small><h1>{title}</h1><p>{description}</p></div></div><div className="ja-view" key={tab}>{notice&&<p className="admin-notice">{notice}</p>}{tab==="pulse"&&<AdminPulse records={records} registrations={registrations}/>} {tab==="review"&&<><section className="admin-test-guide"><div><small>如何测试审核闭环</small><h2>企业提交 → JA 审核排序 → 学生端公开</h2></div><ol><li><b>1</b><span>在<a href="/workspace?role=enterprise" target="_blank">企业端</a>新建内容并提交</span></li><li><b>2</b><span>回到此页设置分类、排序数字和推荐位</span></li><li><b>3</b><span>通过后到<a href="/workspace?role=student" target="_blank">学生端</a>查看</span></li></ol><button onClick={load}>刷新待审核列表</button></section><div className="admin-records">{loading?<div className="admin-empty">正在读取审核队列…</div>:pending.length===0?<div className="admin-empty">当前没有待审核内容，请先从企业端提交一条测试内容</div>:pending.map(r=><RecordCard key={r.id} item={r} onOpen={()=>{setSelectedId(r.id);setReason("")}}/>)}</div></>} {tab==="publish"&&<AdminPublisher onPublished={async message=>{setNotice(message);await load()}}/>}{tab==="registrations"&&<RegistrationTable items={registrations} onSelect={setSelectedRegistration} onRefresh={load}/>} {tab==="records"&&<div className="admin-records records-view">{publicRecords.map(r=><RecordCard key={r.id} item={r} onOpen={()=>setSelectedId(r.id)}/>)}</div>}{tab==="integrity"&&<IntegrityDesk items={blacklist} onSaved={async message=>{setNotice(message);await load()}}/>}{tab==="audit"&&<div className="admin-table"><div><b>时间</b><b>动作</b><b>对象</b><b>操作者</b></div>{logs.map(l=><div key={l.id}><span>{new Date(l.createdAt).toLocaleString("zh-CN")}</span><span>{l.action}</span><span>{l.targetType} · {l.targetId}</span><span>{l.actorId.slice(0,18)}…</span></div>)}</div>}</div></section></div>{selected&&<ReviewDrawer selected={selected} reason={reason} setReason={setReason} decide={decide} close={()=>{setSelectedId("");setReason("")}}/>}{selectedRegistration&&<RegistrationDrawer item={selectedRegistration} onClose={()=>setSelectedRegistration(null)}/>}</main>
}

function RecordCard({item,onOpen}:{item:RecordItem;onOpen:()=>void}){return <article><span>{kindName(item.kind)}</span><div><small>{String(item.payload.company||item.payload.category||"湖南平台内容")} · 排序 {orderOf(item)}{item.payload.featured?" · 首页推荐":""}</small><h2>{String(item.payload.title||"未命名内容")}</h2><p>{stateName(item.payload.reviewStatus)} · {String(item.payload.summary||"无简介")}</p><em>更新于 {new Date(String(item.updatedAt)).toLocaleString("zh-CN")}</em></div><button className="inspect" onClick={onOpen}>查看详情</button></article>}

function AdminPulse({records,registrations}:{records:RecordItem[];registrations:Registration[]}){const jobs=records.filter(r=>r.kind==="job"&&r.payload.reviewStatus==="approved").length,acts=records.filter(r=>r.kind==="activity"&&r.payload.reviewStatus==="approved").length,cont=records.filter(r=>r.kind==="content"&&r.payload.reviewStatus==="approved").length,approved=registrations.filter(r=>r.status==="approved").length;return <><div className="metrics admin-pulse-metrics"><Metric label="实习 / 项目机会" value={String(jobs)} note="湖南企业公开机会"/><Metric label="成长活动" value={String(acts)} note="可报名或可回顾活动"/><Metric label="报名转化" value={`${registrations.length?Math.round(approved/registrations.length*100):0}%`} note="企业确认通过比例"/><Metric label="成长内容" value={String(cont)} note="技能、分享与企业曝光"/></div><section className="platform-map"><div><small>LIVE VALUE MAP</small><h2>平台从机会供给，到成长沉淀</h2><p>这里先用现有数据做动态演示：企业发布内容，JA 审核排序，学生报名后形成成长记录，并逐步沉淀就业、实习、项目产出。</p></div><ol><li><b>{jobs+acts}</b><span>企业供给</span></li><li><b>{registrations.length}</b><span>学生参与</span></li><li><b>{approved}</b><span>确认通过</span></li><li><b>{Math.max(12,cont*3)}</b><span>成长沉淀</span></li></ol></section></>}
function Metric({label,value,note}:{label:string;value:string;note:string}){return <article className="metric aqua"><span>{label}</span><b>{value}</b><p>{note}</p></article>}

function AdminBlockEditor({blocks,setBlocks}:{blocks:RichBlock[];setBlocks:(blocks:RichBlock[])=>void}){const update=(id:string,patch:Partial<RichBlock>)=>setBlocks(blocks.map(block=>block.id===id?{...block,...patch}:block));const add=(type:RichBlock["type"])=>setBlocks([...blocks,{id:crypto.randomUUID(),type,title:type==="heading"?"新小标题":type==="agenda"?"09:00-10:00 环节名称":"",text:type==="text"?"输入正文内容。":type==="quote"?"输入重点提示。":""}]);const move=(id:string,offset:number)=>{const index=blocks.findIndex(block=>block.id===id),next=index+offset;if(index<0||next<0||next>=blocks.length)return;const copy=[...blocks];const [item]=copy.splice(index,1);copy.splice(next,0,item);setBlocks(copy)};return <section className="content-studio-editor admin-editor"><div className="editor-toolbar"><button onClick={()=>add("heading")}>H2 小标题</button><button onClick={()=>add("text")}>正文段落</button><button onClick={()=>add("quote")}>重点提示</button><button onClick={()=>add("gallery")}>图集</button><button onClick={()=>add("agenda")}>议程</button><button onClick={()=>add("card")}>业务卡片</button><button onClick={()=>add("attachment")}>附件</button></div><div className="studio-block-list">{blocks.map((block,index)=><article key={block.id} className={`studio-block block-${block.type}`}><div className="block-head"><span>{block.type}</span><nav><button onClick={()=>move(block.id,-1)} disabled={index===0}>↑</button><button onClick={()=>move(block.id,1)} disabled={index===blocks.length-1}>↓</button><button onClick={()=>setBlocks(blocks.filter(x=>x.id!==block.id))}>删除</button></nav></div>{block.type==="text"||block.type==="quote"?<textarea rows={block.type==="text"?4:2} value={block.text||""} onChange={e=>update(block.id,{text:e.target.value})}/>:<input value={block.title||""} onChange={e=>update(block.id,{title:e.target.value})} placeholder="标题、议程或附件名称"/>}{block.type==="gallery"&&<textarea rows={3} value={(block.items||[]).join("\n")} onChange={e=>update(block.id,{items:e.target.value.split("\n").map(x=>x.trim()).filter(Boolean)})} placeholder="每行一张图片链接"/>}{(block.type==="image"||block.type==="video"||block.type==="attachment")&&<input value={block.url||""} onChange={e=>update(block.id,{url:e.target.value})} placeholder="媒体或附件链接"/>}</article>)}</div></section>}

function AdminPreview({item}:{item:{title:string;summary:string;cover:string;coverType?:string;category?:string;date?:string;place?:string;bodyBlocks:RichBlock[];abilityTags?:string[];registrationFields?:Field[]}}){return <article className="content-renderer admin-content-preview">{item.coverType==="video"?<video src={item.cover} controls/>:<img src={item.cover} alt={item.title}/>}<small>{item.category||"Star Plan"}</small><h1>{item.title||"未命名内容"}</h1><p className="lead">{item.summary||"摘要会展示在学生端列表和详情页。"}</p>{item.date&&<div className="render-meta"><span>{item.date}</span><span>{item.place}</span></div>}{item.abilityTags?.length?<div className="ability-chip-row">{item.abilityTags.map(tag=><span key={tag}>{tag}</span>)}</div>:null}<div className="render-blocks">{item.bodyBlocks.map(block=><section key={block.id} className={`render-block render-${block.type}`}>{block.type==="heading"&&<h2>{block.title}</h2>}{block.type==="text"&&<p>{block.text}</p>}{block.type==="quote"&&<blockquote>{block.text}</blockquote>}{block.type==="agenda"&&<ol className="render-agenda">{String(block.title||"").split("｜").map(line=><li key={line}>{line}</li>)}</ol>}{block.type==="card"&&<div className="render-card"><b>{block.title}</b><p>{block.text}</p></div>}{block.type==="gallery"&&<div className="render-gallery">{(block.items||["/media/ja-career-fair.jpg","/media/ja-competition.jpg"]).map(url=><img key={url} src={url} alt="图集图片"/>)}</div>}{block.type==="attachment"&&<a className="render-attachment" href={block.url||"#"}>📎 {block.title||"附件"}</a>}</section>)}</div>{item.registrationFields?.length?<section className="render-extra"><h2>报名字段</h2>{item.registrationFields.map(field=><span key={field.id}>{field.label}{field.required?" *":""}</span>)}</section>:null}</article>}

function AdminPublisher({onPublished}:{onPublished:(message:string)=>Promise<void>}){
 const [kind,setKind]=useState<"activity"|"content">("activity"),[saving,setSaving]=useState(false),[fields,setFields]=useState(["name","phone","school"]),[custom,setCustom]=useState(""),[step,setStep]=useState("基础信息"),[abilities,setAbilities]=useState(["表达沟通","行业认知"]),[blocks,setBlocks]=useState<RichBlock[]>(adminStarterBlocks.activity),[form,setForm]=useState({title:"",summary:"",place:"湖南 · 长沙",date:"",capacity:"100",category:"技能成长",duration:"10 分钟",mediaType:"article",cover:"/media/ja-career-fair.jpg",coverType:"image",sortOrder:"0",featured:false,agenda:"09:00-09:30 签到\n09:30-11:00 主题体验\n11:00-12:00 复盘输出"});
 const change=(key:string,value:string|boolean)=>setForm(v=>({...v,[key]:value}));
 const switchKind=(next:"activity"|"content")=>{setKind(next);setStep("基础信息");setBlocks(adminStarterBlocks[next]);setForm(v=>({...v,category:next==="activity"?"技能成长":"技能成长"}))};
 const media=async(file?:File)=>{if(!file)return;const data=new FormData();data.append("file",file);data.append("purpose","media");const response=await fetch("/api/files",{method:"POST",body:data});const result=await response.json();if(response.ok&&result.url)setForm(v=>({...v,cover:result.url,coverType:String(result.type).startsWith("video/")?"video":"image",mediaType:String(result.type).startsWith("video/")?"video":v.mediaType}))};
 const publish=async()=>{if(!form.title.trim()||!form.summary.trim())return;setSaving(true);const registrationFields=[...fieldOptions.filter(x=>fields.includes(x.id)),...custom.split("\n").map(x=>x.trim()).filter(Boolean).map((label,index)=>({id:`custom_${index+1}`,label,type:"textarea",required:false}))];const base={title:form.title,summary:form.summary,category:form.category,cover:form.cover,coverType:form.coverType,bodyBlocks:blocks,abilityTags:abilities,sortOrder:Number(form.sortOrder)||0,featured:form.featured,plainText:[form.title,form.summary,...blocks.map(block=>`${block.title||""} ${block.text||""}`)].join("\n")};const payload=kind==="activity"?{...base,place:form.place.includes("湖南")?form.place:`湖南 · ${form.place}`,date:form.date||"待定",capacity:Number(form.capacity)||100,registered:0,status:"已发布",registrationFields,agenda:form.agenda.split("\n").map(x=>x.trim()).filter(Boolean),publisher:"JA China"}:{...base,duration:form.duration,level:"入门",mediaType:form.mediaType,tags:abilities,publisher:"JA China"};const response=await fetch("/api/admin",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({kind,payload})});const result=await response.json();setSaving(false);if(!response.ok){await onPublished(result.error||"发布失败");return}setForm(v=>({...v,title:"",summary:""}));await onPublished("JA 内容已直接发布，学生端刷新后即可看到")};
 const registrationFields=[...fieldOptions.filter(x=>fields.includes(x.id)),...custom.split("\n").map(x=>x.trim()).filter(Boolean).map((label,index)=>({id:`custom_${index+1}`,label,type:"textarea",required:false}))];
 return <section className="admin-publisher studio-admin-publisher"><div className="admin-publisher-intro"><small>DIRECT PUBLISH · CONTENT STUDIO</small><h2>由 JA 直接发布活动或成长内容</h2><p>JA 可用结构化字段 + 富媒体内容块发布活动、文章或视频。活动可设置报名表、议程、能力目标和首页推荐。</p></div><div className="publish-kind"><button className={kind==="activity"?"active":""} onClick={()=>switchKind("activity")}>成长活动</button><button className={kind==="content"?"active":""} onClick={()=>switchKind("content")}>文章 / 视频</button></div><nav className="studio-steps">{(kind==="activity"?["基础信息","活动详情","报名设置","成长设计","预览"]:["基础信息","正文排版","预览"]).map(name=><button key={name} className={step===name?"active":""} onClick={()=>setStep(name)}>{name}</button>)}</nav><div className="admin-studio-grid"><div className="admin-publisher-fields">{step==="基础信息"&&<><label>标题<input value={form.title} onChange={e=>change("title",e.target.value)} placeholder={kind==="activity"?"活动名称":"内容标题"}/></label><label>分类<select value={form.category} onChange={e=>change("category",e.target.value)}>{contentCategories.map(c=><option key={c}>{c}</option>)}</select></label><label>排序数字<input type="number" value={form.sortOrder} onChange={e=>change("sortOrder",e.target.value)} placeholder="数字越大越靠前"/></label><label className="switch-line"><input type="checkbox" checked={form.featured} onChange={e=>change("featured",e.target.checked)}/><span>首页推荐展示</span></label><label className="full">摘要<textarea rows={4} value={form.summary} onChange={e=>change("summary",e.target.value)} placeholder="请清晰说明活动或内容的价值"/></label>{kind==="activity"?<><label>活动地点<input value={form.place} onChange={e=>change("place",e.target.value)}/></label><label>活动日期<input type="date" value={form.date} onChange={e=>change("date",e.target.value)}/></label><label>人数上限<input type="number" min="1" value={form.capacity} onChange={e=>change("capacity",e.target.value)}/></label></>:<><label>阅读 / 观看时长<input value={form.duration} onChange={e=>change("duration",e.target.value)}/></label><label>内容形式<select value={form.mediaType} onChange={e=>change("mediaType",e.target.value)}><option value="article">图文文章</option><option value="video">视频</option></select></label></>}<label className="media-upload full">封面图片或视频<input type="file" accept="image/*,video/mp4,video/webm" onChange={e=>media(e.target.files?.[0])}/><span>{form.cover?"✓ 已设置封面":"选择文件"}</span></label></>}{(step==="活动详情"||step==="正文排版")&&<AdminBlockEditor blocks={blocks} setBlocks={setBlocks}/>} {step==="报名设置"&&<fieldset className="registration-config full"><legend>报名表需要学生填写</legend><div>{fieldOptions.map(field=><label key={field.id}><input type="checkbox" checked={fields.includes(field.id)} onChange={()=>setFields(v=>v.includes(field.id)?v.filter(x=>x!==field.id):[...v,field.id])}/><span>{field.label}</span><small>{field.required?"必填":"选填"}</small></label>)}</div><label>自定义问题（每行一个）<textarea rows={3} value={custom} onChange={e=>setCustom(e.target.value)}/></label></fieldset>} {step==="成长设计"&&<><label className="full">活动议程（每行一个环节）<textarea rows={5} value={form.agenda} onChange={e=>change("agenda",e.target.value)}/></label><fieldset className="ability-picker full"><legend>预期培养维度</legend>{abilityOptions.map(tag=><label key={tag}><input type="checkbox" checked={abilities.includes(tag)} onChange={()=>setAbilities(v=>v.includes(tag)?v.filter(x=>x!==tag):[...v,tag])}/><span>{tag}</span></label>)}</fieldset></>} {step==="预览"&&<AdminPreview item={{...form,bodyBlocks:blocks,abilityTags:abilities,registrationFields}}/>}</div><aside className="studio-preview"><b>学生端实时预览</b><AdminPreview item={{...form,bodyBlocks:blocks,abilityTags:abilities,registrationFields}}/></aside></div><button className="admin-publish-button" disabled={saving} onClick={publish}>{saving?"正在发布…":"立即发布到学生端 →"}</button></section>
}

function IntegrityDesk({items,onSaved}:{items:RecordItem[];onSaved:(message:string)=>Promise<void>}){const [form,setForm]=useState({target:"",type:"学生",summary:"",level:"观察"}),[saving,setSaving]=useState(false);const change=(key:string,value:string)=>setForm(v=>({...v,[key]:value}));const save=async()=>{if(!form.target.trim()||!form.summary.trim())return;setSaving(true);const response=await fetch("/api/admin",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({kind:"blacklist",payload:{title:`${form.type}诚信记录：${form.target}`,summary:form.summary,target:form.target,type:form.type,level:form.level}})});const result=await response.json();setSaving(false);if(!response.ok){await onSaved(result.error||"记录失败");return}setForm({target:"",type:"学生",summary:"",level:"观察"});await onSaved("诚信记录已保存")};return <section className="integrity-desk"><div className="integrity-form"><small>HONESTY PRINCIPLE</small><h2>诚信记录</h2><p>用于测试阶段记录虚假信息、失约、恶意提交等情况。正式上线后可接入学生或企业账号限制。</p><label>对象名称<input value={form.target} onChange={e=>change("target",e.target.value)} placeholder="学生姓名 / 企业名称"/></label><label>对象类型<select value={form.type} onChange={e=>change("type",e.target.value)}><option>学生</option><option>企业</option></select></label><label>处理等级<select value={form.level} onChange={e=>change("level",e.target.value)}><option>观察</option><option>限制操作</option><option>黑名单永久保留</option></select></label><label>记录原因<textarea rows={4} value={form.summary} onChange={e=>change("summary",e.target.value)}/></label><button disabled={saving} onClick={save}>{saving?"正在保存…":"保存诚信记录"}</button></div><div className="integrity-list">{items.length===0?<p>暂无诚信记录。</p>:items.map(item=><article key={item.id}><b>{String(item.payload.target||item.payload.title)}</b><span>{String(item.payload.type)} · {String(item.payload.level)}</span><p>{String(item.payload.summary)}</p></article>)}</div></section>}

function RegistrationTable({items,onSelect,onRefresh}:{items:Registration[];onSelect:(item:Registration)=>void;onRefresh:()=>void}){return <><div className="admin-data-head"><div><b>{items.length}</b><span>条活动报名</span></div><p>JA 可查看全部活动报名；企业只会看到自己发布活动的数据。</p><button onClick={onRefresh}>刷新数据</button></div>{items.length===0?<div className="admin-empty">还没有学生提交报名信息</div>:<div className="registration-table"><div className="registration-row head"><b>活动</b><b>姓名</b><b>联系电话</b><b>学校</b><b>详情</b></div>{items.map(item=><div className="registration-row" key={item.id}><strong>{item.activityTitle}</strong><span>{item.answers.name||"未填写"}</span><span>{item.answers.phone||"未填写"}</span><span>{item.answers.school||"未填写"}</span><button onClick={()=>onSelect(item)}>查看完整报名表</button></div>)}</div>}</>}

function RegistrationDrawer({item,onClose}:{item:Registration;onClose:()=>void}){return <div className="admin-review-drawer registration-drawer"><button className="drawer-close" onClick={onClose}>×</button><small>活动报名详情</small><h1>{item.activityTitle}</h1><div className="registration-answer-list">{Object.entries(item.answers).map(([key,value])=><p key={key}><b>{({name:"姓名",phone:"联系电话",email:"邮箱",school:"学校与专业",studentId:"学号",expectation:"活动期待"} as Record<string,string>)[key]||key}</b><span>{value||"未填写"}</span></p>)}</div><p className="review-result">提交时间：{new Date(item.createdAt).toLocaleString("zh-CN")}</p></div>}

function ReviewDrawer({selected,reason,setReason,decide,close}:{selected:RecordItem;reason:string;setReason:(value:string)=>void;decide:(decision:"approved"|"rejected",settings?:{sortOrder:number;category:string;featured:boolean})=>void;close:()=>void}){const [sortOrder,setSortOrder]=useState(String(selected.payload.sortOrder||0)),[category,setCategory]=useState(String(selected.kind==="job"?selected.payload.jobCategory||jobCategories[0]:selected.payload.category||contentCategories[0])),[featured,setFeatured]=useState(Boolean(selected.payload.featured));const choices=selected.kind==="job"?jobCategories:contentCategories;const blocks=(selected.payload.bodyBlocks as RichBlock[]|undefined)||[];return <div className="admin-review-drawer"><button className="drawer-close" onClick={close}>×</button><small>{kindName(selected.kind)}审核详情</small><h1>{String(selected.payload.title||"未命名")}</h1><div className="review-meta"><span><b>发布方</b>{String(selected.payload.company||selected.payload.publisher||"湖南平台")}</span>{selected.kind==="job"&&<><span><b>岗位类别</b>{String(selected.payload.jobCategory||"未选择")}</span><span><b>区域</b>{String(selected.payload.region||selected.payload.city||"湖南")}</span><span><b>招聘邮箱</b>{String(selected.payload.contactEmail||"未填写")}</span></>}</div>{selected.payload.cover&&<img src={String(selected.payload.cover)} alt="发布方上传的审核素材"/>}<div className="review-settings"><label>分类<select value={category} onChange={e=>setCategory(e.target.value)}>{choices.map(item=><option key={item}>{item}</option>)}</select></label><label>排序数字<input type="number" value={sortOrder} onChange={e=>setSortOrder(e.target.value)} /></label><label className="switch-line"><input type="checkbox" checked={featured} onChange={e=>setFeatured(e.target.checked)}/><span>首页推荐</span></label></div><h3>简介</h3><p>{String(selected.payload.summary||"无")}</p>{blocks.length>0&&<AdminPreview item={{title:String(selected.payload.title||""),summary:String(selected.payload.summary||""),cover:String(selected.payload.cover||"/media/ja-career-fair.jpg"),coverType:String(selected.payload.coverType||"image"),category:String(selected.kind==="job"?selected.payload.jobCategory||"":selected.payload.category||""),date:String(selected.payload.date||""),place:String(selected.payload.place||selected.payload.city||""),bodyBlocks:blocks,abilityTags:selected.payload.abilityTags as string[]|undefined,registrationFields:selected.payload.registrationFields as Field[]|undefined}}/>}{Array.isArray(selected.payload.agenda)&&<><h3>活动议程</h3><ul>{(selected.payload.agenda as string[]).map(x=><li key={x}>{x}</li>)}</ul></>}{Array.isArray(selected.payload.registrationFields)&&<><h3>活动报名字段</h3><ul>{(selected.payload.registrationFields as Field[]).map(field=><li key={field.id}>{field.label} · {field.required?"必填":"选填"}</li>)}</ul></>}{Array.isArray(selected.payload.responsibilities)&&<><h3>岗位职责</h3><ul>{selected.payload.responsibilities.map(x=><li key={String(x)}>{String(x)}</li>)}</ul></>}{Array.isArray(selected.payload.requirements)&&<><h3>能力要求</h3><ul>{selected.payload.requirements.map(x=><li key={String(x)}>{String(x)}</li>)}</ul></>}<label>JA 审核意见<textarea rows={4} value={reason} onChange={e=>setReason(e.target.value)} placeholder="通过时可填写确认意见；退回时必须说明修改内容"/></label>{(!selected.payload.reviewStatus||selected.payload.reviewStatus==="pending")?<div className="drawer-actions"><button onClick={()=>decide("rejected",{sortOrder:Number(sortOrder)||0,category,featured})}>退回企业修改</button><button className="approve" onClick={()=>decide("approved",{sortOrder:Number(sortOrder)||0,category,featured})}>审核通过并公开</button></div>:<p className="review-result">当前状态：{stateName(selected.payload.reviewStatus)}<br/>{String(selected.payload.reviewNote||"")}</p>}</div>}
