# 星光计划单机迁移 Implementation Plan

> **For agentic workers:** Execute task-by-task using superpowers:executing-plans. Sites checkout edits remain with the owning agent; independent reviewers may inspect but not edit.

**Goal:** Run the latest platform on the user's Tencent Lighthouse server without losing source records, uploads or original credentials.

**Architecture:** Preserve the Vinext/React app and existing SQL. Add a Node-target build that substitutes SQLite and private-disk adapters for the Cloudflare environment. Keep the existing Sites build available for rollback. Nginx terminates TLS; a systemd service binds Node to loopback only.

**Tech Stack:** Existing locked Vinext, React; Node 24 LTS, node:sqlite, SQLite WAL, private filesystem, Nginx, systemd, Certbot.

**Spec:** User approved SQLite single-server architecture in this conversation; migration evidence at `../../../../06_部署迁移/2026-09-04-迁移核验记录.md`.

## Global Constraints

- Domain `star-plan.com`; ICP `湘ICP备2026038303号-1`; contact `ruthyanghao@hotmail.com`.
- No fake public-security filing number; mail delivery stays unconfigured.
- Keep all original records, credential hashes and AUTH_PEPPER. Do not print secrets or commit them.
- Default production test mode OFF. Preserve authenticated routes and private resume authorization.
- Source snapshot remains immutable. Restore only into new destinations. Do not change DNS before server acceptance.
- Existing compliance edits are preserved. Source Sites hosting metadata and lockfile remain intact.

## Task 1: Node persistence compatibility

Files: `server/sqlite.mjs`, `server/files.mjs`, `server/environment.mjs`, `tests/self-host-storage.test.mjs`.

Interfaces: `openDatabase(path)` returns D1-compatible prepare/bind/first/all/raw/run/batch/exec plus close; `createFileStore(path)` returns put/get. `env` exposes lazy DB/FILES and whitelisted auth/mail configuration.

- [x] Write real file/SQLite tests: persisted binds, first(null), changes, batch rollback, retained sequences, private metadata, binary integrity, path traversal rejection.
- [x] Run `node --test tests/self-host-storage.test.mjs` and observe missing adapter failure.
- [x] Implement prepared statements, synchronous atomic batch, WAL/foreign keys/busy timeout; atomic metadata pointer to immutable hash-addressed blobs outside public directories.
- [x] Verify those tests and the existing suite; never trust request headers for role or host in production.

## Task 2: Portable build and verified restore

Files: `vite.config.ts`, `next.config.ts`, `scripts/build-node.mjs`, `scripts/restore-export.mjs`, `tests/self-host-restore.test.mjs`.

Interfaces: `restoreExport(sourceDirectory,newDestination)` validates archive digest, row totals and every file digest before writing; restores schema/records/sequences, returns summary and leaves completion manifest only after integrity checks. CLI accepts source and new destination.

- [x] Tests require invalid hash and existing destination refusal; valid fixture preserves quoted strings, ids, private ownership and sequence maxima.
- [x] Implement importer, apply remaining existing migrations based on exported platform migration names, write migration ledger; no new business seeds beyond existing app behavior.
- [x] Build with `STARLIGHT_RUNTIME=node`, no Cloudflare plugin; alias only `cloudflare:workers` to local env. Output standalone using existing framework support.
- [x] Restore production copy locally, start loopback service with original secrets read from private JSON, verify login/roles and private files using HTTP. Test copy only; original snapshot untouched.

## Task 3: Server installation and acceptance

Files: `deploy/star-plan.service`, `deploy/nginx-http.conf`, `scripts/backup.mjs`, `deploy/star-plan-backup.service`, `deploy/star-plan-backup.timer`.

- [x] Install Node LTS from official release checksum, Nginx and Certbot from Ubuntu packages. Dedicated non-login `star-plan` user.
- [x] Upload validated standalone release, adapters/importer and encrypted source backup over SSH. Load secrets from `/etc/star-plan/runtime.json` (root:star-plan 640), never from public assets.
- [x] Restore a fresh `/srv/star-plan/data` copy. Service loopback `127.0.0.1:3000`, restart on failure, restricted filesystem writes.
- [x] Verify local server home, student/enterprise/admin sign-in, authorized API access, wrong-role denial and migrated files. Test temporary changes only in a cloned acceptance database, not in final restored data.
- [x] Backup via SQLite online backup; preserve immutable files/metadata, verify checksum and integrity; systemd daily schedule. Keep original encrypted snapshot separately.

## Task 4: Domain and TLS cutover

- [x] Resolve root/www DNS and inspect 80/443 reachability. 80 reachable; public 443 timed out despite Nginx listening and local TLS succeeding. Requested user check Lighthouse firewall TCP 443.
- [x] Confirm old source has no new data since snapshot. All 18 visible tables read without truncation; row counts and canonical SHA-256 match except the known consumed migration-transfer rate-limit marker. No business changes found.
- [x] User set root/www A to `81.71.71.252`; public DNS confirmed. HTTP and HTTPS www redirect to canonical root. No unrelated DNS records modified.
- [x] Issue certificate using webroot challenge, enforce HTTPS after certificate verification; renewal dry run including Nginx reload hook succeeded.
- [ ] Final verify HTTPS pages, ICP footer link, original credentials and uploads. Report exact remaining blockers if any; do not call IP-only staging a completed domain launch.

## Verification ledger

- Baseline: 104 tests passed before self-host changes; existing dirty changes are approved compliance footer edits.
- Temporary source export already removed; no further old-site publication is authorized by this plan except final synchronization separately approved as needed.
- Final local regression: 115/115 passing; default Sites build and Node standalone build both successful; source lint clean.
- Ubuntu 24.04 / Node 24.20.0: 11/11 self-host tests passing. Production smoke verifies original credentials, role boundaries, secure proxy cookies, ICP footer and database integrity.
- First scheduled-service backup completed and verified, timer enabled. DNS and server-side TLS are configured; final public HTTPS verification remains blocked by port 443 connectivity. See `docs/self-hosting-runbook.md`.
